describe('Amazon.de page', () => {
// I

   /* it('should have the right title', () => {
        browser.url('https://www.amazon.de');
        expect(browser).toHaveTitle('Amazon.de: Günstige Preise für Elektronik & Foto, Filme, Musik, Bücher, Games, Spielzeug & mehr');
        
    })

    it('should click on "Sichere Anmmeldung" button', () => {
        
        const button = $('#a-autoid-0-announce'); //id
        //expect(button).toBeDisplayed(); //toBeDisplayed
        button.click(); //click
    })

    it('should check if "Hilfe" button exists', () => {

        const button = $('.a-link-normal'); //class
        button.isExisting();

    })*/

// II

   /* it('should click on "Prime" button', () => {
        
        browser.url("https://www.amazon.de/");
        const button = $('.nav-line-2');
        button.click();
       
    })

   it('should check if "Impressum" button is displayed', () => {

   
        const impresie = $('.nav_a')
        expect(impresie).isDisplayed;
   })

   it('should check if "Amazon Pay" button is exists', () => {

        const pay = $ ('.nav_a');
        expect(pay).isExisting;

   }) */

// III

 // it('should check "Ebay" title', () => {

  //  browser.url('https://www.ebay.com');
   // const title = browser.getTitle();
  //  expect(browser).toHaveTitle('Electronics, Cars, Fashion, Collectibles & More | eBay'); 

//})
 /*it('should click on "Search" button', () => {

    const button = $('#gh-btn');
    button.click();

})

it('should contain "Electronics" button', () =>{

    const button = $('.ttl');
    expect(button).isDisplayed;
}) */

// IV
/*
it('should click on "Twitter" button', () => {

    browser.url('https://www.ebay.com');
    const button = $('//*[@id="gf-BIG"]/table/tbody/tr/td[3]/ul/li[7]/a');
    button.click();
})

it('should check "Follow" button if exists', () => {

    const button = $('.css-18t94o4 css-1dbjc4n r-1niwhzg r-p1n3y5 r-sdzlij r-1phboty r-rs99b7 r-1w2pmg r-1vuscfd r-1dhvaqw r-1fneopy r-o7ynqc r-6416eg r-lrvibr');
    expect(button).isExisting;
})

it('should check "Tweet" button is displayed', () => {

    const button = $('.css-901oao css-16my406 r-1qd0xha r-ad9z0x r-bcqeeo r-qvutc0');
    expect(button).isDisplayed;
}) */

//V

/*it('should click on "Careers" button', () => {

    browser.url('https://www.amazon.com');
    const button = $('#navFooter > div.navFooterVerticalColumn > div > div:nth-child(1) > ul > li.nav_first > a');
    button.click();

})

it('should check if "discover roles" button is displayed', () => {

    const button = $('#homepage-campaign-banner-btn');
    expect(button).isDisplayed;

})

it('should check if "Learn about working at Amazon" button is exists', () =>{

    const button = $('.btn.btn-tertiary.color-neutral');
    expect(button).isExisting;
})

it('should have the right title', () => {
    
    const title = browser.getTitle();
    expect(browser).toHaveTitle('Amazon.jobs: Help us build Earth’s most customer-centric company.');

}) */

})